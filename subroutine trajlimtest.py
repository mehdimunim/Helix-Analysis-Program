def trajlimtest(nframe, MAXFRAMES):
	if (nframe > MAXFRAMES):
		print(' Calculation stopped since this option is limited to reading  frames Recompile with larger value of the parameter MAXFRAMES')
		
